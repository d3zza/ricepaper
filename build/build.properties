#Properties file for build.xml
project.name=ricepaper-framework

#Version number
ricepaper.ver.num=v0.0.1
project.name.versioned=${project.name}-${ricepaper.ver.num}

#build locations
asdoc.loc=${FLEX_HOME}/bin/asdoc
main.src.loc=${basedir}/src
test.src.loc=${basedir}/src_test
doc.loc=${basedir}/doc
lib.loc=${basedir}/lib
bin.loc=${basedir}/bin
report.loc=${basedir}/report
dist.loc=${basedir}/dist
template.loc=${basedir}/build/templates
script.loc=${basedir}/script
#Project links
